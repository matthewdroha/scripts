.\" Automatically generated by Pod::Man v1.37, Pod::Parser v1.14
.\"
.\" Standard preamble:
.\" ========================================================================
.de Sh \" Subsection heading
.br
.if t .Sp
.ne 5
.PP
\fB\\$1\fR
.PP
..
.de Sp \" Vertical space (when we can't use .PP)
.if t .sp .5v
.if n .sp
..
.de Vb \" Begin verbatim text
.ft CW
.nf
.ne \\$1
..
.de Ve \" End verbatim text
.ft R
.fi
..
.\" Set up some character translations and predefined strings.  \*(-- will
.\" give an unbreakable dash, \*(PI will give pi, \*(L" will give a left
.\" double quote, and \*(R" will give a right double quote.  | will give a
.\" real vertical bar.  \*(C+ will give a nicer C++.  Capital omega is used to
.\" do unbreakable dashes and therefore won't be available.  \*(C` and \*(C'
.\" expand to `' in nroff, nothing in troff, for use with C<>.
.tr \(*W-|\(bv\*(Tr
.ds C+ C\v'-.1v'\h'-1p'\s-2+\h'-1p'+\s0\v'.1v'\h'-1p'
.ie n \{\
.    ds -- \(*W-
.    ds PI pi
.    if (\n(.H=4u)&(1m=24u) .ds -- \(*W\h'-12u'\(*W\h'-12u'-\" diablo 10 pitch
.    if (\n(.H=4u)&(1m=20u) .ds -- \(*W\h'-12u'\(*W\h'-8u'-\"  diablo 12 pitch
.    ds L" ""
.    ds R" ""
.    ds C` ""
.    ds C' ""
'br\}
.el\{\
.    ds -- \|\(em\|
.    ds PI \(*p
.    ds L" ``
.    ds R" ''
'br\}
.\"
.\" If the F register is turned on, we'll generate index entries on stderr for
.\" titles (.TH), headers (.SH), subsections (.Sh), items (.Ip), and index
.\" entries marked with X<> in POD.  Of course, you'll have to process the
.\" output yourself in some meaningful fashion.
.if \nF \{\
.    de IX
.    tm Index:\\$1\t\\n%\t"\\$2"
..
.    nr % 0
.    rr F
.\}
.\"
.\" For nroff, turn off justification.  Always turn off hyphenation; it makes
.\" way too many mistakes in technical documents.
.hy 0
.if n .na
.\"
.\" Accent mark definitions (@(#)ms.acc 1.5 88/02/08 SMI; from UCB 4.2).
.\" Fear.  Run.  Save yourself.  No user-serviceable parts.
.    \" fudge factors for nroff and troff
.if n \{\
.    ds #H 0
.    ds #V .8m
.    ds #F .3m
.    ds #[ \f1
.    ds #] \fP
.\}
.if t \{\
.    ds #H ((1u-(\\\\n(.fu%2u))*.13m)
.    ds #V .6m
.    ds #F 0
.    ds #[ \&
.    ds #] \&
.\}
.    \" simple accents for nroff and troff
.if n \{\
.    ds ' \&
.    ds ` \&
.    ds ^ \&
.    ds , \&
.    ds ~ ~
.    ds /
.\}
.if t \{\
.    ds ' \\k:\h'-(\\n(.wu*8/10-\*(#H)'\'\h"|\\n:u"
.    ds ` \\k:\h'-(\\n(.wu*8/10-\*(#H)'\`\h'|\\n:u'
.    ds ^ \\k:\h'-(\\n(.wu*10/11-\*(#H)'^\h'|\\n:u'
.    ds , \\k:\h'-(\\n(.wu*8/10)',\h'|\\n:u'
.    ds ~ \\k:\h'-(\\n(.wu-\*(#H-.1m)'~\h'|\\n:u'
.    ds / \\k:\h'-(\\n(.wu*8/10-\*(#H)'\z\(sl\h'|\\n:u'
.\}
.    \" troff and (daisy-wheel) nroff accents
.ds : \\k:\h'-(\\n(.wu*8/10-\*(#H+.1m+\*(#F)'\v'-\*(#V'\z.\h'.2m+\*(#F'.\h'|\\n:u'\v'\*(#V'
.ds 8 \h'\*(#H'\(*b\h'-\*(#H'
.ds o \\k:\h'-(\\n(.wu+\w'\(de'u-\*(#H)/2u'\v'-.3n'\*(#[\z\(de\v'.3n'\h'|\\n:u'\*(#]
.ds d- \h'\*(#H'\(pd\h'-\w'~'u'\v'-.25m'\f2\(hy\fP\v'.25m'\h'-\*(#H'
.ds D- D\\k:\h'-\w'D'u'\v'-.11m'\z\(hy\v'.11m'\h'|\\n:u'
.ds th \*(#[\v'.3m'\s+1I\s-1\v'-.3m'\h'-(\w'I'u*2/3)'\s-1o\s+1\*(#]
.ds Th \*(#[\s+2I\s-2\h'-\w'I'u*3/5'\v'-.3m'o\v'.3m'\*(#]
.ds ae a\h'-(\w'a'u*4/10)'e
.ds Ae A\h'-(\w'A'u*4/10)'E
.    \" corrections for vroff
.if v .ds ~ \\k:\h'-(\\n(.wu*9/10-\*(#H)'\s-2\u~\d\s+2\h'|\\n:u'
.if v .ds ^ \\k:\h'-(\\n(.wu*10/11-\*(#H)'\v'-.4m'^\v'.4m'\h'|\\n:u'
.    \" for low resolution devices (crt and lpr)
.if \n(.H>23 .if \n(.V>19 \
\{\
.    ds : e
.    ds 8 ss
.    ds o a
.    ds d- d\h'-1'\(ga
.    ds D- D\h'-1'\(hy
.    ds th \o'bp'
.    ds Th \o'LP'
.    ds ae ae
.    ds Ae AE
.\}
.rm #[ #] #H #V #F C
.\" ========================================================================
.\"
.IX Title "HSDES::Api::Article 3"
.TH HSDES::Api::Article 3 "2017-07-26" "perl v5.8.5" "User Contributed Perl Documentation"
.Sh "newChildArticle Creates an empty hash resembling the child subject Must pass in childSubject name =cut sub newChildArticle {"
.IX Subsection "newChildArticle Creates an empty hash resembling the child subject Must pass in childSubject name =cut sub newChildArticle {"
.Vb 5
\&        # must be called on a OBJECT
\&        my $self = shift;
\&        unless (ref $self) {
\&                croak HSDES::Api::Error::INVALID_CALL_NOTATION;
\&        }
.Ve
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 5
\&        my $childSubject = shift;
\&        if (!defined $childSubject) {
\&                $self->{_lastErr} = "Must provided a child subject name\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        return _newChildRecord($self,$childSubject);
\&}
.Ve
.ie n .Sh "newApproval Creates an empty hash resembling an approval =cut sub newApproval { # must be called on a \s-1OBJECT\s0 my $self\fP = shift; unless (ref \f(CW$self) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.el .Sh "newApproval Creates an empty hash resembling an approval =cut sub newApproval { # must be called on a \s-1OBJECT\s0 my \f(CW$self\fP = shift; unless (ref \f(CW$self\fP) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.IX Subsection "newApproval Creates an empty hash resembling an approval =cut sub newApproval { # must be called on a OBJECT my $self = shift; unless (ref $self) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        return _newChildRecord($self,"approval");
\&}
.Ve
.PP
sub newAR {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        return _newChildRecord($self,"ar");
\&}
.Ve
.PP
sub newAttachment {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        return _newChildRecord($self,"document");
\&}
.Ve
.PP
sub newComment {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        return _newChildRecord($self,"comments");
\&}
.Ve
.PP
sub _newChildRecord {
	my \f(CW$self\fR = shift;
	my \f(CW$childName\fR = shift;
.PP
.Vb 3
\&        my $tenant = $self->{_data}->{tenant};
\&        my $parentID = $self->{_data}->{id};
\&        my $subject = $childName;
.Ve
.PP
.Vb 3
\&        # try to get all field names
\&        my $viewPortName = $tenant . "." . $childName;
\&        my @validFields = $_apiHandle->_getCache()->getAllFields($viewPortName);
.Ve
.PP
.Vb 10
\&        if (!@validFields) {
\&                # unable to load field info
\&                $self->{_lastErr} = $_apiHandle->_getCache()->getLastErrorMessage();
\&                return 0;
\&        }
\&        # make a empty hash and send that back to user
\&        my %emptyChildHash = ();
\&        foreach my $singlField (@validFields) {
\&                $emptyChildHash{$singlField} = "";
\&        }
.Ve
.PP
.Vb 4
\&        # set these fields by default
\&        $emptyChildHash{"tenant"} = $tenant;
\&        $emptyChildHash{"subject"} = $subject;
\&        $emptyChildHash{"parent_id"} = $parentID;
.Ve
.PP
.Vb 6
\&        # load the rule engine for this tenant/subject
\&        # run the onNewRecord EVENTS (such as setting some default values)
\&        my $combinedRules = HSDES::Api::RuleHelper::RuleUtil::GetRules($_apiHandle, $tenant, $subject);
\&        my $delegate = new HSDES::Api::RuleHelper::Delegate($_apiHandle);
\&        my $ruleEngineObj = new HSDES::RuleEngine($delegate);
\&        $ruleEngineObj->setRules($combinedRules);
.Ve
.PP
.Vb 6
\&        # register customer managers
\&        my $stateManager = new HSDES::Api::RuleHelper::StateManager();
\&        my $changeManager = new HSDES::RuleEngine::ChangeManager();
\&        my $clickManager = new HSDES::RuleEngine::ClickManager();
\&        $ruleEngineObj->registerWidgets($stateManager, $changeManager, $clickManager);
\&        $ruleEngineObj->wireUp();
.Ve
.PP
.Vb 4
\&        # give the current hash to the delegate/statemanager so it can SET some values
\&        $delegate->setData(\e%emptyChildHash);
\&        $stateManager->setData(\e%emptyChildHash);
\&        $ruleEngineObj->onNewRecord();
.Ve
.PP
.Vb 1
\&        tie %emptyChildHash, 'HSDES::Api::ESHash', \e%emptyChildHash, $changeManager, $clickManager, $stateManager, $_apiHandle->_getCache();
.Ve
.PP
.Vb 2
\&        return \e%emptyChildHash;
\&}
.Ve
.PP
sub newArticle {
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
	my \f(CW$tenant\fR = shift;
	my \f(CW$subject\fR = shift;
.PP
.Vb 4
\&        if (!defined $tenant) {
\&                $self->{_lastErr} = "Must define tenant to create new article.";
\&                return 0;
\&        }
.Ve
.PP
.Vb 4
\&        if (!defined $subject) {
\&                $self->{_lastErr} = "Must define SUBJECT to create new article.";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        my $viewPortName = $tenant . "." . $subject;
\&        my @validFields = $_apiHandle->_getCache()->getAllFields($viewPortName);
.Ve
.PP
.Vb 5
\&        if (!(@validFields)) {
\&                # unable to load field info
\&                $self->{_lastErr} = $_apiHandle->_getCache()->getLastErrorMessage();
\&                return 0;
\&        }
.Ve
.PP
.Vb 5
\&        # make a empty hash and send that back to user
\&        my %emptyHash = ();
\&        foreach my $singlField (@validFields) {
\&                $emptyHash{$singlField} = "";
\&        }
.Ve
.PP
.Vb 3
\&        # set these fields by default
\&        $emptyHash{"tenant"} = $tenant;
\&        $emptyHash{"subject"} = $subject;
.Ve
.PP
.Vb 6
\&        # load the rule engine for this tenant/subject
\&        # run the onNewRecord EVENTS (such as setting some default values)
\&        my $combinedRules = HSDES::Api::RuleHelper::RuleUtil::GetRules($_apiHandle, $tenant, $subject);
\&        my $delegate = new HSDES::Api::RuleHelper::Delegate($_apiHandle);
\&        my $ruleEngineObj = new HSDES::RuleEngine($delegate);
\&        $ruleEngineObj->setRules($combinedRules);
.Ve
.PP
.Vb 6
\&        # register customer managers
\&        $_stateManager = new HSDES::Api::RuleHelper::StateManager();
\&        my $changeManager = new HSDES::RuleEngine::ChangeManager();
\&        my $clickManager = new HSDES::RuleEngine::ClickManager();
\&        $ruleEngineObj->registerWidgets($_stateManager, $changeManager, $clickManager);
\&        $ruleEngineObj->wireUp();
.Ve
.PP
.Vb 4
\&        # give the current hash to the delegate/statemanager so it can SET some values
\&        $delegate->setData(\e%emptyHash);
\&        $_stateManager->setData(\e%emptyHash);
\&        $ruleEngineObj->onNewRecord();
.Ve
.PP
.Vb 2
\&        # pass these along to the hash
\&        tie %emptyHash, 'HSDES::Api::ESHash', \e%emptyHash, $changeManager, $clickManager, $_stateManager, $_apiHandle->_getCache();
.Ve
.PP
.Vb 2
\&        return \e%emptyHash;
\&}
.Ve
.ie n .Sh "getChildArticles Must pass in the name of the child subject =cut sub getChildArticles { # must be called on a \s-1OBJECT\s0 my $self\fP = shift; unless (ref \f(CW$self) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.el .Sh "getChildArticles Must pass in the name of the child subject =cut sub getChildArticles { # must be called on a \s-1OBJECT\s0 my \f(CW$self\fP = shift; unless (ref \f(CW$self\fP) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.IX Subsection "getChildArticles Must pass in the name of the child subject =cut sub getChildArticles { # must be called on a OBJECT my $self = shift; unless (ref $self) { croak HSDES::Api::Error::INVALID_CALL_NOTATION; }"
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 5
\&        my $childSubject = shift;
\&        if (!defined $childSubject) {
\&                $self->{_lastErr} = "Must provided a child subject name\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        my $parentID = $self->{_data}->{id};
\&        my $searchText = "[parent_id] = $parentID";
.Ve
.PP
.Vb 3
\&        my $response = HSDES::Api::Util::callToWService($_apiHandle, "Viewport","execute", {viewportName=>$childSubject,searchText=>$searchText});
\&        my $isSuccess = HSDES::Api::Util::IS_SUCCESS($response);
\&        if ($isSuccess) {
.Ve
.PP
.Vb 6
\&                my @childArray = ();
\&                foreach my $singleAR (@{$response->{DATA}}) {
\&                        HSDES::Api::Util::REMOVE_SYSTEM_FIELDS($singleAR);
\&                        my $arOBj = HSDES::Api::ChildRecord->new($_apiHandle, $singleAR);
\&                        push (@childArray, $arOBj);
\&                }
.Ve
.PP
.Vb 5
\&                return @childArray;
\&        } else {
\&                $self->{_lastErr} = HSDES::Api::Util::GET_ERROR_MSG($response);
\&                return $isSuccess;
\&        }
.Ve
.PP
}
.PP
sub getApprovals {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        my $id = $self->{_data}->{id};
\&        my $tenant = $self->{_data}->{tenant};
.Ve
.PP
.Vb 3
\&        my $response = HSDES::Api::Util::callToWService($_apiHandle, "Record","getApprovals", {id=>$id,tenant=>$tenant});
\&        my $isSuccess = HSDES::Api::Util::IS_SUCCESS($response);
\&        if ($isSuccess) {
.Ve
.PP
.Vb 6
\&                my @approvalArray = ();
\&                foreach my $singleAR (@{$response->{DATA}}) {
\&                        HSDES::Api::Util::REMOVE_SYSTEM_FIELDS($singleAR);
\&                        my $arOBj = HSDES::Api::ChildRecord->new($_apiHandle, $singleAR);
\&                        push (@approvalArray, $arOBj);
\&                }
.Ve
.PP
.Vb 5
\&                return @approvalArray;
\&        } else {
\&                $self->{_lastErr} = HSDES::Api::Util::GET_ERROR_MSG($response);
\&                return $isSuccess;
\&        }
.Ve
.PP
}
.PP
sub getARS {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        my $id = $self->{_data}->{id};
\&        my $tenant = $self->{_data}->{tenant};
.Ve
.PP
.Vb 3
\&        my $response = HSDES::Api::Util::callToWService($_apiHandle, "Record","getArs", {id=>$id,tenant=>$tenant});
\&        my $isSuccess = HSDES::Api::Util::IS_SUCCESS($response);
\&        if ($isSuccess) {
.Ve
.PP
.Vb 6
\&                my @arArray = ();
\&                foreach my $singleAR (@{$response->{DATA}}) {
\&                        HSDES::Api::Util::REMOVE_SYSTEM_FIELDS($singleAR);
\&                        my $arOBj = HSDES::Api::ChildRecord->new($_apiHandle, $singleAR);
\&                        push (@arArray, $arOBj);
\&                }
.Ve
.PP
.Vb 6
\&                return @arArray;
\&        } else {
\&                $self->{_lastErr} = HSDES::Api::Util::GET_ERROR_MSG($response);
\&                return $isSuccess;
\&        }
\&}
.Ve
.PP
sub getComments {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 5
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "Must call LOAD FIRST\en";
\&                return 0;
\&        }
.Ve
.PP
.Vb 2
\&        my $id = $self->{_data}->{id};
\&        my $tenant = $self->{_data}->{tenant};
.Ve
.PP
.Vb 3
\&        my $response = HSDES::Api::Util::callToWService($_apiHandle, "Record","getComments", {id=>$id,tenant=>$tenant});
\&        my $isSuccess = HSDES::Api::Util::IS_SUCCESS($response);
\&        if ($isSuccess) {
.Ve
.PP
.Vb 6
\&                my @commentArray = ();
\&                foreach my $singleComment (@{$response->{DATA}}) {
\&                        HSDES::Api::Util::REMOVE_SYSTEM_FIELDS($singleComment);
\&                        my $commentObj = HSDES::Api::ChildRecord->new($_apiHandle, $singleComment);
\&                        push (@commentArray, $commentObj);
\&                }
.Ve
.PP
.Vb 6
\&                return @commentArray;
\&        } else {
\&                $self->{_lastErr} = HSDES::Api::Util::GET_ERROR_MSG($response);
\&                return $isSuccess;
\&        }
\&}
.Ve
.PP
sub getLinkedRecords {
	# must be called on a \s-1OBJECT\s0
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
.PP
.Vb 16
\&        # do we have data?
\&        if (!defined $self->{_data}) {
\&                $self->{_lastErr} = "must call LOAD first\en";
\&                return 0;
\&        }
\&        my $id = $self->{_data}->{id};
\&        my $response = HSDES::Api::Util::callToWService($_apiHandle, "Record","getRelatedRecords", {id=>$id});
\&        my $isSuccess = HSDES::Api::Util::IS_SUCCESS($response);
\&        if ($isSuccess) {
\&                my $data = $response->{DATA};
\&                return $data;
\&        } else {
\&                $self->{_lastErr} = HSDES::Api::Util::GET_ERROR_MSG($response);
\&                return $isSuccess;
\&        }
\&}
.Ve
.PP
sub getLastErrorMessage {
	my \f(CW$self\fR = shift;
	unless (ref \f(CW$self\fR) {
		croak HSDES::Api::Error::INVALID_CALL_NOTATION;
	}
	return \f(CW$self\fR\->{_lastErr};
}
.PP
sub _containsBinaryField {
	my \f(CW$self\fR = shift;
	my \f(CW$viewportName\fR = shift;
	my \f(CW$data\fR = shift;
.PP
.Vb 2
\&        # Binary fields
\&        my @binaryFields = $_apiHandle->_getCache()->getBinaryFields($viewportName);
.Ve
.PP
.Vb 8
\&        # Check if data has binary field
\&        foreach my $field (keys %{$data}) {
\&                if (HSDES::Api::Util::DOES_KEY_EXIST_IN_ARRAY(\e@binaryFields, $field)) {
\&                        return 1;
\&                }
\&        }
\&        return 0;
\&}
.Ve
.PP
sub fieldMissing {
	my \f(CW$self\fR = shift;
	my \f(CW$data\fR = shift;
	my \f(CW$tenant\fR = \f(CW$data\fR\->{tenant};
	my \f(CW$subject\fR = \f(CW$data\fR\->{subject};
.PP
.Vb 8
\&        # fire the btnSave event
\&        my $secretKey = HSDES::Api::ESHash::MARKERFORINTERNALFIELDS . "CLICKMANAGER";
\&        my $clickManager = $data->{$secretKey};
\&        eval {$clickManager->onButtonClicked("btnSave");};
\&        if($@) {
\&                $self->{_lastErr} = $@;
\&                return 0;
\&        }
.Ve
.PP
.Vb 9
\&        $secretKey = HSDES::Api::ESHash::MARKERFORINTERNALFIELDS . "STATEMANAGER";
\&        my $stateManager = $data->{$secretKey};
\&        my $requiredFields = $stateManager->getRequiredFields();
\&        my @missingRequiredFields = ();
\&        for my $field (keys %$requiredFields) {
\&                if ($data->{$field} eq '') {
\&                        push (@missingRequiredFields, $field);
\&                }
\&        }
.Ve
.PP
.Vb 6
\&        if (scalar(@missingRequiredFields)) {
\&                $self->{_lastErr} = "The following fields are required for $tenant.$subject: " . join(',',@missingRequiredFields);
\&                return 0;
\&        }
\&        return 1;
\&}
.Ve
.SH "SYNOPSIS This subj will be used to do some data validation before calling the MiddleTier. As of ww21_2013; this is only checking the People/Person fields"
.IX Header "SYNOPSIS This subj will be used to do some data validation before calling the MiddleTier. As of ww21_2013; this is only checking the People/Person fields"
.Vb 4
\&        This is being called from
\&                1) update
\&                2) insert
\&                3) _insertChild
.Ve
.PP
.Vb 6
\&        Author: nsshergi
\&=cut
\&sub validChanges {
\&        my $self         = shift;
\&        my $viewportName = shift;
\&        my $changes      = shift;
.Ve
.PP
.Vb 5
\&        # we are only going to check the People/Person fields
\&        # iterate over all changes and do further checks if
\&        # one of the changed field is of type person/people
\&        my @personFields = $_apiHandle->_getCache()->getPersonFields($viewportName);
\&        my @peopleFields = $_apiHandle->_getCache()->getPeopleFields($viewportName);
.Ve
.PP
.Vb 12
\&        # Check user modified fields; mainly Person/People fields
\&        foreach my $field (keys %$changes) {
\&                # what type of field is this?
\&                if (HSDES::Api::Util::DOES_KEY_EXIST_IN_ARRAY(\e@personFields, $field)) {
\&                        # this is a single person. There should be no comma/semicolon in here
\&                        # Technically we should be able tos end the value as is to BE and have it return true/false
\&                        # but BE SP treats semicolon separated idsid as valid. We have to break it up :(
\&                        my $valueToCheck = $changes->{$field};
\&                        if ($valueToCheck =~ /\e;/) {
\&                                $self->{_lastErr} = "Error: field '$field' cannot contain ';'.";
\&                                return 0;
\&                        }
.Ve
.PP
.Vb 4
\&                        if ($valueToCheck =~ /\e,/) {
\&                                $self->{_lastErr} = "Error: field '$field' cannot contain ','.";
\&                                return 0;
\&                        }
.Ve
.PP
.Vb 5
\&                        # whatever we get now; assume its single idsid and let db validate it
\&                        if(!HSDES::Api::Util::IsValidIDSID($_apiHandle, $valueToCheck)) {
\&                                $self->{_lastErr} = "Error: $valueToCheck is an invalid idsid for [$field]";
\&                                return 0;
\&                        }
.Ve
.PP
.Vb 1
\&                } # if person field
.Ve
.PP
.Vb 9
\&                if (HSDES::Api::Util::DOES_KEY_EXIST_IN_ARRAY(\e@peopleFields, $field)) {
\&                        # This must be a comma separated list of idsid
\&                        # Technically we should be able tos end the value as is to BE and have it return true/false
\&                        # but BE SP treats semicolon separated idsid as valid. We have to break it up :(
\&                        my $valueToCheck = $changes->{$field};
\&                        if ($valueToCheck =~ /\e;/) {
\&                                $self->{_lastErr} = "Error: field '$field' cannot contain ';'.";
\&                                return 0;
\&                        }
.Ve
.PP
.Vb 10
\&                        # what is left; split it on a comma and verify each one
\&                        my @people = split(",", $changes->{$field});
\&                        foreach my $person (@people) {
\&                                if(!HSDES::Api::Util::IsValidIDSID($_apiHandle, $person)) {
\&                                        $self->{_lastErr} = "Error: $person is an invalid idsid for [$field]";
\&                                        return 0;
\&                                }
\&                        }
\&                } # if people field
\&        } # for each changed field
.Ve
.PP
.Vb 2
\&        return 1;
\&}
.Ve
.PP
sub getReadOnlyFields {
	my \f(CW$self\fR = shift;
.PP
.Vb 6
\&        if(!$_stateManager) {
\&                $self->{_lastErr} = "Error: unable to get read only fields.\enPlease call load() or newArticle() first.";
\&                return undef;
\&        }
\&        return $_stateManager->getReadOnlyFields();
\&}
.Ve
.PP
sub getHiddenFields {
	my \f(CW$self\fR = shift;
.PP
.Vb 6
\&        if(!$_stateManager) {
\&                $self->{_lastErr} = "Error: unable to get hidden fields.\enPlease call load() or newArticle() first.";
\&                return undef;
\&        }
\&        return $_stateManager->getHiddenFields();
\&}
.Ve
.PP
sub getRequiredFields {
	my \f(CW$self\fR = shift;
.PP
.Vb 6
\&        if(!$_stateManager) {
\&                $self->{_lastErr} = "Error: unable to get required fields.\enPlease call load() or newArticle() first.";
\&                return undef;
\&        }
\&        return $_stateManager->getRequiredFields();
\&}
.Ve
.PP
sub getLookupFields {
	my \f(CW$self\fR = shift;
.PP
.Vb 6
\&        if(!$_stateManager) {
\&                $self->{_lastErr} = "Error: unable to get lookup fields.\enPlease call load() or newArticle() first.";
\&                return undef;
\&        }
\&        return $_stateManager->getFilterLookups();
\&}
.Ve
.PP
1;
